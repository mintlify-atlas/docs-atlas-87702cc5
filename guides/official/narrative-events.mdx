---
title: "Narrative Events"
description: "Create pop-up narrative events with choices, requirements, and linked stories."
---

<Note>
  This guide assumes familiarity with [Database Modding](/guides/official/database-modding) and the [Modifier System](/guides/official/modifier-system). If you haven't modded before, start with [Getting Started](/guides/official/getting-started).
</Note>

**Narrative Events** are the pop-ups that appear as you play, presenting story moments and choices. With modding you can create your own stories and bring them into Civilization VII.

## Anatomy of a narrative event

Most pop-ups are composed of **multiple Narrative Event entries** linked together. A single pop-up with three choices involves four entries: one for the pop-up and one for each button.

Narrative events break down into two sections:

**Pop-up content:**
- **StoryTitle** — title at the top of the pop-up
- **Completion** — the body flavor text (shown when the event triggers)

**Button content:**
- **Name** — key flavor text on the button
- **Imperative** — optional command text shown below the Name
- **Description** — tooltip text describing the gameplay effect

<Note>
  Exception: if a Narrative Event does **not** lead to further events, the pop-up shows a single "Close" button. The button text is pulled from the **Description** of the same event (not a separate entry). Name and Imperative are ignored in this case.
</Note>

---

## Creating events

### Define the NarrativeStories

For a pop-up with two choices, create three events and link them:

```xml
<?xml version="1.0" encoding="utf-8"?>
<Database>
  <Types>
    <Row Type="EVENT_A" Kind="KIND_NARRATIVE_STORY"/>
    <Row Type="EVENT_B" Kind="KIND_NARRATIVE_STORY"/>
    <Row Type="EVENT_C" Kind="KIND_NARRATIVE_STORY"/>
  </Types>
  <NarrativeStories>
    <Row
      NarrativeStoryType="EVENT_A"
      Name="LOC_EVENT_A_NAME"
      Description="LOC_EVENT_A_DESCRIPTION"
      Completion="LOC_EVENT_A_COMPLETION"
      StoryTitle="LOC_EVENT_A_STORYTITLE"
      Age="AGE_ANTIQUITY"
      Activation="REQUISITE"
      ActivationRequirementSetId="REQSET_EVENT_A_NARRATIVE_REQUISITE"
      RequirementSetId="REQSET_EVENT_A_NARRATIVE"
      UIActivation="STANDARD"
    />
    <Row
      NarrativeStoryType="EVENT_B"
      Name="LOC_EVENT_B_NAME"
      Description="LOC_EVENT_B_DESCRIPTION"
      Completion="LOC_EVENT_B_COMPLETION"
      Age="AGE_ANTIQUITY"
      Activation="LINKED"
      RequirementSetId="Met"
      UIActivation="STANDARD"
    />
    <Row
      NarrativeStoryType="EVENT_C"
      Name="LOC_EVENT_C_NAME"
      Description="LOC_EVENT_C_DESCRIPTION"
      Completion="LOC_EVENT_C_COMPLETION"
      StoryTitle="LOC_EVENT_C_STORYTITLE"
      Age="AGE_ANTIQUITY"
      Activation="LINKED"
      IsQuest="FALSE"
      Hidden="TRUE"
      RequirementSetId="Met"
      UIActivation="STANDARD"
    />
  </NarrativeStories>
  <NarrativeStory_Links>
    <Row
      FromNarrativeStoryType="EVENT_A"
      ToNarrativeStoryType="EVENT_B"
      Name="LOC_EVENT_B_NAME"
      Description="LOC_EVENT_B_DESCRIPTION"
    />
    <Row
      FromNarrativeStoryType="EVENT_A"
      ToNarrativeStoryType="EVENT_C"
      Name="LOC_EVENT_C_NAME"
      Description="LOC_EVENT_C_DESCRIPTION"
    />
  </NarrativeStory_Links>
</Database>
```

### Column reference

<AccordionGroup>
  <Accordion title="Name / Description">
    Must be defined even if unused. Provide dummy localization tags — they cannot be empty.
  </Accordion>

  <Accordion title="UIActivation">
    Controls the visual style of the pop-up. `STANDARD` and `DISCOVERY` are the most common choices.
  </Accordion>

  <Accordion title="Activation">
    How the event is triggered:

    - `REQUISITE` — for the initial event in a chain. Checked against `RequirementSetId` continuously.
    - `LINKED` — for events that appear as buttons on a parent event.
    - `LINKED_REQUISITE` — like `LINKED`, but also checks `ActivationRequirementSetId` when the parent triggers.
    - `AUTO` — for initial Discovery events.
  </Accordion>

  <Accordion title="ActivationRequirementSetId (optional)">
    A `RequirementSet` checked **once** when the event is initialised. Events that fail are immediately archived and never shown.

    For `REQUISITE` events, use this for stable checks like civilizationType.
    For `LINKED_REQUISITE` events, this is checked at the moment the parent pop-up triggers — useful for conditional button visibility.
    Events with `Activation="LINKED"` ignore this field.
  </Accordion>

  <Accordion title="RequirementSetId">
    Checked continuously after initialisation. When the requirement is met, the event is marked 'complete' and its pop-up is shown.

    Set to `Met` if you don't need any condition.
  </Accordion>

  <Accordion title="IsQuest (optional)">
    Marks the event as a quest. Defaults to `FALSE`. Does not automatically add a Quest icon to buttons — add that separately via `NarrativeRewardIcons`.
  </Accordion>

  <Accordion title="Hidden (optional)">
    `TRUE` = the event doesn't spawn a pop-up when complete, but still appears as a button in events that lead to it. Defaults to `FALSE`.
  </Accordion>
</AccordionGroup>

---

## Requirements

Use RequirementSets to control who sees an event and when.

For an event that triggers when a player founds their 2nd city — but only if they're playing as Rome, Aksum, or the Mississippians:

- `ActivationRequirementSetId` checks civilization type once at initialisation.
- `RequirementSetId` checks whether the player has founded 2 cities, re-evaluated continuously.

```xml
<?xml version="1.0" encoding="utf-8"?>
<GameEffects xmlns="GameEffects">
  <RequirementSet id="REQSET_EVENT_A_NARRATIVE_REQUISITE" type="REQUIREMENTSET_TEST_ANY">
    <Requirement type="REQUIREMENT_PLAYER_CIVILIZATION_TYPE_MATCHES">
      <Argument name="CivilizationType">CIVILIZATION_AKSUM</Argument>
    </Requirement>
    <Requirement type="REQUIREMENT_PLAYER_CIVILIZATION_TYPE_MATCHES">
      <Argument name="CivilizationType">CIVILIZATION_ROME</Argument>
    </Requirement>
    <Requirement type="REQUIREMENT_PLAYER_CIVILIZATION_TYPE_MATCHES">
      <Argument name="CivilizationType">CIVILIZATION_MISSISSIPPIAN</Argument>
    </Requirement>
  </RequirementSet>
  <RequirementSet id="REQSET_EVENT_A_NARRATIVE">
    <Requirement type="REQUIREMENT_PLAYER_HAS_AT_LEAST_NUM_GOSSIPS">
      <Argument name="GossipTypes">GOSSIP_FOUND_CITY01</Argument>
      <Argument name="DuplicateCount">2</Argument>
    </Requirement>
  </RequirementSet>
</GameEffects>
```

<Tip>
  `REQUIREMENT_PLAYER_HAS_AT_LEAST_NUM_GOSSIPS` is a flexible requirement type that overlaps with many standard requirements. The city founding check above could also use `REQUIREMENT_PLAYER_HAS_FOUNDED_X_CITIES`. Which you choose depends on your preferences and the specific condition you need.
</Tip>

---

## Loading in .modinfo

Load your narrative event files with `UpdateDatabase` in the game scope. Requirements defined as `<GameEffects>` XML go in a separate file but can share the same `ActionGroup`:

```xml
<ActionGroups>
  <ActionGroup id="antiquity-game" scope="game" criteria="antiquity-age-current">
    <Actions>
      <UpdateDatabase>
        <Item>data/antiquity-discovery.xml</Item>
        <Item>data/antiquity-discovery-game-effects.xml</Item>
      </UpdateDatabase>
      <UpdateText>
        <Item>text/antiquity-discovery-text.xml</Item>
      </UpdateText>
    </Actions>
  </ActionGroup>
</ActionGroups>
```
